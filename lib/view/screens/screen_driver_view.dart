import 'package:flutter/material.dart';

class ScreenDriverView extends StatelessWidget {
  const ScreenDriverView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('driver view'),
      ),
    );
  }
}
