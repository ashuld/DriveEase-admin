import 'package:flutter/material.dart';

class ScreenDriverLIst extends StatelessWidget {
  const ScreenDriverLIst({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('driverlist'),
      ),
    );
  }
}
